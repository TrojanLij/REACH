"""Deprecated compatibility module.

Forge generator commands now live in reach.cli.forge.generator.
"""

from __future__ import annotations

# Intentionally empty: commands are registered from generator.py with
# both `generator` and backward-compatible `payload` command names.
