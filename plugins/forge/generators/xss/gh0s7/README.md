# Gh0st XSS (Forge Generator)

Generates the Gh0st XSS payload with optional command execution and optional `<script>` tags.

## Usage

```bash
reach forge generator new xss_gh0st --dry-run --generator-kwarg command=id
```
