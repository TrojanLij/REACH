# `reach dev`

Developer utilities for local environments.

## Clear in-memory request logs
```bash
reach dev clear-logs
```

## Reset database schema
```bash
reach dev reset-db --yes
```

`reset-db` drops and recreates all tables. Treat as destructive.

